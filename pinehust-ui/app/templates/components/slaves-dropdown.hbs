<li class="dropdown">
    <a class="dropdown-toggle" data-toggle="dropdown" href="#" {{action "click"}}>
        <button type="button" class="btn btn-default btn-circle"><i class="fa fa-volume-up fa-fw"></i></button>
        <i style="color:black;" class="fa fa-caret-down"></i>
    </a>
    <ul class="dropdown-menu dropdown-messages">
      {{#each slavesList as |slave|}}
        {{slaves-dropdown-item model=slave}}
      {{/each}}

        <li>
            <a>
                <div>
                    <strong>ton ordi</strong>
                  {{#if audio_player.isPlaying}}
                      <i class="fa fa-play fa-fw"></i>
                  {{else if audio_player.isPaused}}
                      <i class="fa fa-pause fa-fw"></i>
                  {{else if audio_player.isStopped}}
                      <i class="fa fa-stop fa-fw"></i>
                  {{/if}}
                    <span class="pull-right text-muted">
          <em>Sylvain la pute</em>
        </span>


                </div>
                <div>{{audio_player.playingTrack.name}}</div>
                <input id="ordinateur" type="range" min="0" max="1" step="0.1"
                       value="0.5" {{action "onVolumeUpdateLocal"}}/>
                <button {{action 'muteLocal'  bubbles=false}} type="button" class="btn btn-default btn-circle btn-sm">
                    <i class="fa fa-volume-off fa-fw"></i>
                </button>


            </a>
        </li>
        <li class="divider"></li>

        <li>
            <a class="text-center" href="#">
                <strong>See more details</strong>
                <i class="fa fa-angle-right"></i>
            </a>
        </li>
    </ul>
</li>
